#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    char *buf;

    scanf("%d", &n);

    return (0);
}

int getResult(int n, int cnt, int num)
{
    char    *buffer;
    int     digit;

    while (cnt < 1000000)
    {
                
    }
}

